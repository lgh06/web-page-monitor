{
  "name": "root",
  "private": true,
  "license": "See monorepo's LICENSE.md",
  "scripts": {
    "init": "lerna bootstrap",
    "setpm2":"npm i pm2 -g && pm2 install pm2-logrotate && pm2 set pm2-logrotate:rotateInterval \"0 15 */2 * *\"",
    "postinstall": "npm i lerna@3.22.1 -g && npm run setpm2 && npm run init ",
    "dev-web": "lerna run --scope @webest/web-page-monitor-web dev",
    "dev": "lerna bootstrap && lerna run dev --parallel --no-bail",
    "start": "lerna bootstrap && lerna run start --parallel --no-bail",
    "stop": "lerna run stop"
  }
}
