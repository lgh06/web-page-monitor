/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // experimental: {

  // },
  // webpack: (config) => {
  //   config.externals = [
  //     ...config.externals,
  //     'mongodb',
  //   ]
  //   return config;
  // },
}

module.exports = nextConfig
